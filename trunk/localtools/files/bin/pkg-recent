#!/bin/sh
# Use '-r' for gnu set compatability
# could back from the matching time to handle Linux & NetBSD ls differences
ls -lrt /var/db/pkg/*/+DESC | \
    sed -r -e 's:/[^/]*$::' -e 's:/.*/: :' -e 's/.*(.{7}[0-9][0-9]:)/\1/'
